name: Nomina cohesive stamp temp

on:
  push:
    branches: [ux/nomina-maddy-cohesive]

permissions:
  contents: write

jobs:
  stamp:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ux/nomina-maddy-cohesive
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Sellar release de Nómina
        run: |
          npm run release:stamp
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/release.json public/js/core/release.js
          git commit -m "chore(release): stamp cohesive payroll UX"
          git push origin HEAD:ux/nomina-maddy-cohesive
